package constants

const Domain = "https://budbot.suitsoftware.com"
const LocalDomain = "http://127.0.0.1" // important to use 127.0.0.1 instead of localhost for using telegram widget login in local developemnt

const HourInSeconds = 3600
const HourInMinutes = 60

const WeekInSeconds = 3600 * 24 * 7
const DayInMinutes = 24 * 60
