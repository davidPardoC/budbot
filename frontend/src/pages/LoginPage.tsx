import LoginForm from "../components/auth/login-form/LoginForm"

const LoginPage = () => {
  return (
    <main className="container p-5 flex justify-center min-h-screen items-start py-64">
        <LoginForm />
    </main>
  )
}

export default LoginPage